# Server-only requirements — no whisperx/PyTorch (transcription runs on Mac, not server)
# Audio transcription disabled via TRANSCRIPTION_MODE=app in docker-compose
numpy
rich
pyyaml
fastapi
uvicorn[standard]
python-dotenv
asyncpg>=0.31.0
pgvector>=0.3.0
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1
mcp>=1.0.0
# anthropic>=0.40.0      # Optional: Anthropic provider (llm kind 'anthropic') — pip install anthropic
ddgs>=0.5
apscheduler>=3.10
httpx
python-multipart>=0.0.9 # multipart form parsing — required by FastAPI UploadFile/Form routes
requests
slowapi>=0.1.9
