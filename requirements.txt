whisperx
rich
pyyaml
fastapi
uvicorn[standard]
python-dotenv
asyncpg>=0.31.0
pgvector>=0.3.0
pytest>=8.0
pytest-asyncio>=0.24
aiosmtpd>=1.4         # test SMTP server for the outreach send-worker e2e test
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1
mcp>=1.0.0,<2
# anthropic>=0.40.0      # Optional: Anthropic provider (llm kind 'anthropic') — pip install anthropic
ddgs>=0.5               # web search for enrichment agent (Phase 5; renamed from duckduckgo-search)
apscheduler>=3.10       # heartbeat scheduler (Phase 5)
httpx>=0.27.0           # async HTTP client (used in routers for Pi/browser-service calls)
python-multipart>=0.0.9 # multipart form parsing — required by FastAPI UploadFile/Form routes
slowapi>=0.1.9          # rate limiting middleware (Phase 18)
requests>=2.31.0        # sync HTTP client (used in db.py + agents)
cryptography>=42.0        # AES-GCM for per-org LLM keys at rest (Phase 6a)
matrix-nio[e2e]>=0.26.0     # Matrix federation transport (Phase 5b); vodozemac wheel, no libolm
PyJWT[crypto]>=2.8            # external login (Supabase/OIDC JWT exchange, Phase 6b)
